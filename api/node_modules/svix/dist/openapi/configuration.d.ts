import { HttpLibrary } from "svix/dist/openapi/http/http";
import { Middleware, PromiseMiddleware } from "svix/dist/openapi/middleware";
import { BaseServerConfiguration } from "svix/dist/openapi/servers";
import { AuthMethods, AuthMethodsConfiguration } from "svix/dist/openapi/auth/auth";
export interface Configuration {
    readonly baseServer: BaseServerConfiguration;
    readonly httpApi: HttpLibrary;
    readonly middleware: Middleware[];
    readonly authMethods: AuthMethods;
}
export interface ConfigurationParameters {
    baseServer?: BaseServerConfiguration;
    httpApi?: HttpLibrary;
    middleware?: Middleware[];
    promiseMiddleware?: PromiseMiddleware[];
    authMethods?: AuthMethodsConfiguration;
}
export declare function createConfiguration(conf?: ConfigurationParameters): Configuration;
